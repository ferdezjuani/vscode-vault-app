/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // env: {
  //   API_URL: "http://localhost:1337",
  // },
  images: {
    domains: ["utfs.io"],
  },
};

export default nextConfig;
